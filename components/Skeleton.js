function Skeleton({className}) {
    return (
        <>
            <span className={`block ${className} bg-zinc-200 animate-pulse rounded-sm relative overflow-hidden`}></span>
        </>
    )
}

export default Skeleton
