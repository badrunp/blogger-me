function Skeleton({className}) {
    return (
        <>
            <span className={`block ${className} bg-zinc-200 rounded-sm relative overflow-hidden`}></span>
        </>
    )
}

export default Skeleton
